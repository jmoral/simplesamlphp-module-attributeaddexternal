<?xml version="1.0"?>
<psalm
    name="SimpleSAMLphp attributeaddexternal"
    useDocblockTypes="true"
    errorLevel="4"
    reportMixedIssues="false"
    hideExternalErrors="true"
    allowStringToStandInForClass="true"
>
    <projectFiles>
        <directory name="tests" />

        <!-- Ignore certain directories -->
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>

    <issueHandlers>
        <!-- Test classes are entry points invoked by PHPUnit via reflection, not referenced in code -->
        <UnusedClass>
            <errorLevel type="suppress">
                <directory name="tests" />
            </errorLevel>
        </UnusedClass>

        <!-- Test classes are conventionally left extensible, not final -->
        <ClassMustBeFinal>
            <errorLevel type="suppress">
                <directory name="tests" />
            </errorLevel>
        </ClassMustBeFinal>
    </issueHandlers>
</psalm>
